@model List<Microsoft.AspNetCore.Identity.IdentityRole>

<div class="row">
    @await Html.PartialAsync("AdminLeftMenu")

    <div class="col-9">
        <h3>Список ролей</h3>
        <a asp-area="admin" asp-controller="UserRole" asp-action="AddUserRole">Добавить новую роль</a>
        <table class="table-bordered">
            <tr>
                <th>ID</th>
                <th>Наименование</th>
                <th></th>
            </tr>
            @foreach (var item in @Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td><a asp-area="admin" asp-controller="userrole" asp-action="removeuserrole" asp-route-userRoleId="@item.Id">Удалить</a></td>
                </tr>
            }
        </table>
    </div>
</div>